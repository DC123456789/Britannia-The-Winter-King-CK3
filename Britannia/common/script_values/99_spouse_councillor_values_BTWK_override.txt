spouse_martial_event_troop_size = {
	add = martial
	add = prowess
	if = {
		limit = {
			has_trait = strategist
		}
		multiply = 1.5
	}
	if = {
		limit = {
			has_trait = overseer
		}
		multiply = 1.5
	}
	if = {
		limit = {
			has_trait = gallant
		}
		multiply = 1.5
	}

	multiply = 2

	round = yes
}
