create_artifact_dyrnwyn_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_famed = yes

	# Create the artifact
	create_artifact = {
		name = dyrnwyn_name
		description = dyrnwyn_description
		type = pedestal
		visuals = dyrnwyn
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = dyrnwyn_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = dyrnwyn
		save_scope_as = epic
	}
}

create_artifact_gwyddno_garanhirs_hamper_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_famed = yes

	# Create the artifact
	create_artifact = {
		name = gwyddno_garanhirs_hamper_name
		description = gwyddno_garanhirs_hamper_description
		type = box
		visuals = gwyddno_garanhirs_hamper
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = gwyddno_garanhirs_hamper_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = gwyddno_garanhirs_hamper
		save_scope_as = epic
	}
}

create_artifact_bran_galeds_horn_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_famed = yes

	# Create the artifact
	create_artifact = {
		name = bran_galeds_horn_name
		description = bran_galeds_horn_description
		type = pedestal
		visuals = bran_galeds_horn
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = bran_galeds_horn_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = bran_galeds_horn
		save_scope_as = epic
	}
}

create_artifact_chariot_of_morgan_mwynfawr_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_illustrious = yes

	# Create the artifact
	create_artifact = {
		name = chariot_of_morgan_mwynfawr_name
		description = chariot_of_morgan_mwynfawr_description
		type = sculpture
		visuals = chariot_of_morgan_mwynfawr
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = chariot_of_morgan_mwynfawr_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = chariot_of_morgan_mwynfawr
		save_scope_as = epic
	}
}

create_artifact_mantle_of_arthur_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_illustrious = yes

	# Create the artifact
	create_artifact = {
		name = mantle_of_arthur_name
		description = mantle_of_arthur_description
		type = tapestry
		visuals = mantle_of_arthur
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = mantle_of_arthur_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = mantle_of_arthur
		save_scope_as = epic
	}
}

create_artifact_coat_of_padarn_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_famed = yes

	# Create the artifact
	create_artifact = {
		name = coat_of_padarn_name
		description = coat_of_padarn_description
		type = tapestry
		visuals = coat_of_padarn
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = coat_of_padarn_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = coat_of_padarn
		save_scope_as = epic
	}
}

create_artifact_mantle_of_tegau_eurfron_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_famed = yes

	# Create the artifact
	create_artifact = {
		name = mantle_of_tegau_eurfron_name
		description = mantle_of_tegau_eurfron_description
		type = tapestry
		visuals = mantle_of_tegau_eurfron
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = created_before_history
		}
		modifier = mantle_of_tegau_eurfron_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = mantle_of_tegau_eurfron
		save_scope_as = epic
	}
}

create_artifact_halter_of_llawfrodedd_farchog_effect = {
	# Get the character the artifact is being made for.
	$OWNER$ = { save_scope_as = owner }
	# Not really used, but if we don't set the scopes we get errors in the feature selection
	set_artifact_rarity_famed = yes

	# Create the artifact
	create_artifact = {
		name = halter_of_llawfrodedd_farchog_name
		description = halter_of_llawfrodedd_farchog_description
		type = pedestal
		visuals = halter_of_llawfrodedd_farchog
		wealth = scope:wealth
		quality = scope:quality
		history = {
			type = halter_of_llawfrodedd_farchog_history
		}
		modifier = halter_of_llawfrodedd_farchog_modifier
		save_scope_as = newly_created_artifact
		decaying = no
	}

	scope:newly_created_artifact = {
		set_variable = { name = historical_unique_artifact value = yes }
		set_variable = halter_of_llawfrodedd_farchog
		save_scope_as = epic
	}
}