create_variable_struct = {
	random_independent_ruler = {
		create_story = {
			type = variable_struct_story
			save_temporary_scope_as = $NAME$
		}
	}
	set_variable = { name = $NAME$ value = scope:$NAME$ }
}

create_global_variable_struct = {
	random_independent_ruler = {
		create_story = {
			type = variable_struct_story
			save_temporary_scope_as = $NAME$
		}
	}
	set_global_variable = { name = $NAME$ value = scope:$NAME$ }
}